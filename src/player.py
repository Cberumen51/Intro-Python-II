# Write a class to hold player information, e.g. what room they are in
# currently.

class Player:
    # name
    # current_room
    # class
    def __init__(self, current_room=None):
        self.current_room = current_room
        self.inventory = []

    def inventory(self):
        current_inventory = "My inventory"
        for i, item in 

    def drop_item